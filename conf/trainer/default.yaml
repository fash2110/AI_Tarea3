max_epochs: 30
# GPU NVIDIA (RTX 5060 8GB): Lightning usa CUDA vía PyTorch automáticamente
accelerator: gpu
devices: 1
precision: 16-mixed
gradient_clip_val: 1.0
log_every_n_steps: 10
check_val_every_n_epoch: 1
enable_progress_bar: true
